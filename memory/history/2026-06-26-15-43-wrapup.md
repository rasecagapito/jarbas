# Historico - Wrapup Homologacao Jarbas MVP

## Data e Hora

2026-06-26 15:43 -03:00

## Resumo

Sessao consolidou o fluxo GitHub/Vercel/Supabase/n8n do MVP Jarbas Carga PN ate homologacao. O codigo principal foi promovido de `dev` para `hom`, homologacao foi publicada no Vercel, Supabase HOM recebeu usuario/bucket/profile/grupo de teste, e foi criado um PR draft para producao sem fazer merge para `main`.

## Estado Atual

- Branch local atual: `hom`.
- Branch `hom` esta sincronizada com `origin/hom`.
- Working tree possui alteracoes nao relacionadas ao wrapup:
  - `AGENTS.md` modificado com referencia ao comando `/loop`.
  - `commands/loop.md` nao versionado.
- Essas alteracoes foram deixadas intocadas.

## GitHub

- PR `#1` `dev -> hom`: mergeado.
  - URL: `https://github.com/rasecagapito/jarbas/pull/1`
  - Merge em: `2026-06-26T17:57:10Z`.
- Commit de checklist em `hom`: `93721aa docs: update hom validation checklist`.
- PR `#2` `hom -> main`: aberto como draft, sem merge.
  - URL: `https://github.com/rasecagapito/jarbas/pull/2`
  - Status: aberto, draft.

## Vercel

- Preview de homologacao mais recente: `https://jarbas-akvpcmjlz-rasecagapitos-projects.vercel.app`
- Status: `Ready`.
- Preview anterior: `https://jarbas-hufslbems-rasecagapitos-projects.vercel.app`
- Production atual: `https://jarbas-kbxhhq9zs-rasecagapitos-projects.vercel.app`
- Observacao: Preview esta protegido por SSO da Vercel; validacao por `curl` redireciona para camada SSO antes do app.

## Supabase HOM

- Projeto HOM: `qwuhrzmbrapiwdrutjut`.
- Bucket `jarbas-uploads` criado.
- Usuario de teste criado.
- Profile criado.
- Usuario vinculado ao grupo `solucoes`.
- Login validado via API Supabase.
- RLS validada com token do proprio usuario:
  - grupo visivel: `solucoes`;
  - agente executavel: `carga-pn-excel`.
- Credenciais do usuario de teste ficaram somente em arquivo local ignorado pelo Git:
  - `env/.jarbas_hom_test_user`.

## n8n

- Workflow alvo: `AfhcrI8P35wY0SV7`.
- Nome: `JARBAS - SOLUCOES - CARGA PN EXCEL`.
- Versao ativa publicada anteriormente: `33cd2792-05e4-471f-a165-6a0f17c972f8`.
- Endpoint usado pelo portal: `https://tododiasoftware.app.n8n.cloud/webhook/jarbas/carga-pn-excel`.
- Papel atual: adaptador Jarbas para encaminhar payload ao fluxo operacional existente.

## Validacoes Realizadas

- `npm test`: passou antes da promocao, 9 arquivos e 23 testes.
- `npm run build`: passou antes da promocao.
- `npm run lint`: passou antes da promocao.
- Vercel Preview `hom`: `Ready`.
- Login Supabase HOM: validado via API.
- RLS/permissoes do usuario de teste: validado via API REST Supabase.
- Varredura de segredos nos arquivos versionaveis: sem ocorrencias.

## Decisoes

- Nao promover `hom -> main` ainda.
- Manter PR `#2` como draft ate validacao funcional no navegador.
- Nao tentar bypassar SSO da Vercel; a validacao visual/funcional deve ocorrer com navegador logado.
- Nao versionar credenciais locais de teste.
- Manter `env/`, `.env*` e `.vercel/` fora do Git.

## Pendencias

- Validar Preview `hom` em navegador autenticado na Vercel.
- Executar fluxo funcional no app:
  1. abrir Preview de homologacao;
  2. fazer login Jarbas com usuario de teste;
  3. fazer upload XLS/XLSX;
  4. criar execucao;
  5. confirmar chamada ao n8n;
  6. confirmar retorno/logs/status da execucao.
- Confirmar se o fluxo operacional n8n atende completamente os campos enviados pelo adaptador Jarbas.
- Quando homologacao funcional aprovar, tirar PR `#2` de draft e promover `hom -> main`.
- Se homologacao reprovar, corrigir em `dev`, promover novamente para `hom` e retestar.

## Ponto de Retomada

Retomar em `C:\Dev\jarbas\.projeto1`, branch `hom`, lendo este arquivo e o checklist `docs/checklists/implantacao-jarbas-mvp.md`. A proxima melhor acao e validar o Preview `hom` no navegador autenticado da Vercel antes de qualquer merge para `main`.
