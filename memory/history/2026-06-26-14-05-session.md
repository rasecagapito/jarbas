# Historico - Jarbas MVP Auth, Vercel e n8n

## Data e Hora

2026-06-26 14:05

## Resumo

Sessao avancou o MVP Jarbas Carga PN em `dev`, fechando autenticacao real com Supabase, APIs de upload/execucao, cockpit inicial, variaveis Vercel para homologacao/producao e workflow n8n publicado para receber chamadas do portal.

## Trabalho Realizado

- Validados ambientes Supabase de homologacao e producao.
- Aplicadas tabelas/seeds do MVP em homologacao e producao via Supabase Management API.
- Configurado Vercel para `Production` e `Preview (hom)` com Supabase e n8n.
- Reforcado `.gitignore` para manter `env/`, `.env*` e `.vercel/` fora do Git.
- Registrada regra no `AGENTS.md` para tratar `env/` e `.env*` como segredos locais.
- Implementados clients Supabase browser/server e proxy de autenticacao.
- Criada tela de login real com server actions.
- Protegida a home do cockpit por sessao Supabase.
- Criadas APIs `/api/jarbas/upload`, `/api/jarbas/executions` e `/api/jarbas/executions/[id]`.
- Criados componentes iniciais do cockpit Jarbas: painel de agente, composer, voz, upload e progresso de execucao.
- Criados utilitarios de permissoes, roteamento de IA, cliente n8n e voz.
- Publicado workflow n8n `JARBAS - SOLUCOES - CARGA PN EXCEL` como adaptador Jarbas.

## n8n

- Workflow alvo: `AfhcrI8P35wY0SV7`.
- URL no n8n: `https://tododiasoftware.app.n8n.cloud/workflow/AfhcrI8P35wY0SV7`.
- Versao ativa publicada: `33cd2792-05e4-471f-a165-6a0f17c972f8`.
- Endpoint usado pelo portal: `https://tododiasoftware.app.n8n.cloud/webhook/jarbas/carga-pn-excel`.
- Fluxo atual: Webhook Jarbas -> Validar Payload Jarbas -> Encaminhar para Carga PN Operacional -> Responder Jarbas.

## Validacoes

- `npm test`: passou, 9 arquivos e 23 testes.
- `npm run build`: passou com Next.js 16.2.9.
- `npm run lint`: passou com `tsc --noEmit`.
- `vercel env ls preview`: confirmou variaveis n8n em `Preview (hom)`.
- `vercel env ls production`: confirmou variaveis n8n em `Production`.
- `n8n validate_workflow`: passou com 4 nos.
- `n8n publish_workflow`: passou.

## Pendencias

- Fazer commit intencional das alteracoes em `dev`.
- Promover `dev -> hom` via PR ou merge conforme fluxo GitHub definido.
- Disparar deploy de homologacao e executar teste real de login/upload/execucao.
- Confirmar no fluxo operacional n8n se os campos encaminhados pelo adaptador Jarbas atendem toda a carga SAP atual.
- Depois da homologacao aprovada, promover `hom -> main` e validar producao.
