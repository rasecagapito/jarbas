Executa o procedimento provider-neutro em `automation/procedures/handoff.md`.

**Uso**: `/handoff`  (sem argumento = LER o estado atual; "grava" = GRAVAR estado atual)

Estado vivo em `memory/handoff.md` — permite outra IA continuar exatamente onde esta parou.
(Fonte única da lógica partilhada com Codex/Gemini/etc.)
