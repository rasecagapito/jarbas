# /status

Quando o usuario invocar `/status`:
1. Ler o arquivo mais recente em `memory/history/`.
2. Ler os modulos relevantes em `context/`.
3. Identificar os tres principais proximos passos.
4. Apresentar uma tabela compacta com estado, ultima sessao e proximas acoes.

